 summarize_taxa.py -i /mnt/sdb/ww/CloudPlatform/users/17/LF21120133324/otu_taxa_table.biom -o /mnt/sdb/ww/CloudPlatform/users/17/LF21120133324/out -L 6 -a && \ 

sum_tax.pl -i /mnt/sdb/liujing/2020/202010/yanjianna/process/otu_0.97/tax_summary_a/otu_taxa_table_L2.txt -o /mnt/sdb/liujing/2020/202010/yanjianna/process/otu_0.97/tax_summary_a/phylum.xls ;



名字长度大于sector宽度的，自动使用

1. group文件不一定要包含otu丰度里面的所有样本，但必须存在，可以只选取要作图的一部分sample。作图sample部分以group为主

			间隔gap、（没设置的情况下默认c(rep(10, length(all_otu) - 1), 3, rep(10, length(all_sample) - 1), 3)）设置了就直接用
			起始位置270、
			颜色（只设置otuID颜色和样本分组颜色，样本颜色根据对应分组颜色自动渐变）

2. 第一个圈可设置参数：	竖直标签位置调整(vjust 10 mm/cm/inches) @字体最大长度 @字体大小

3.第二个圈参数		是否绘制第二个圈
			百分比字体大小
			三圈高度设置
			第三圈屏蔽otu名


设置作图阈值，筛选作图的otuid

